Flask==3.0.0
flask-cors==4.0.0
python-dotenv==1.0.0
pytz==2024.1
bleak==0.22.2
pandas>=2.0.0
# TensorFlow para PC (versión completa)
tensorflow>=2.15.0; platform_machine != "armv7l" and platform_machine != "aarch64"
# TensorFlow Lite Runtime para Raspberry Pi (intentará usar .tflite, fallará con Flex ops)
# Fallback: Instalará TensorFlow completo si tflite-runtime falla
tflite-runtime; platform_machine == "armv7l" or platform_machine == "aarch64"
# TensorFlow completo como fallback para Raspberry Pi
tensorflow==2.8.0; platform_machine == "armv7l" or platform_machine == "aarch64"
scikit-learn
joblib
numpy
