Flask==3.0.0
flask-cors==4.0.0
python-dotenv==1.0.0
pytz==2024.1
bleak==0.22.2
pandas>=2.0.0
# TensorFlow para PC (versión completa)
tensorflow>=2.15.0; platform_machine != "armv7l" and platform_machine != "aarch64"
# TensorFlow Lite para Raspberry Pi (con SELECT_TF_OPS para LSTM)
# Nota: Se requiere tensorflow completo, no solo tflite-runtime, porque el modelo usa Flex ops
tensorflow==2.13.0; platform_machine == "armv7l" or platform_machine == "aarch64"
scikit-learn
joblib
numpy
